<%= form_for(@lendable) do |f| %>
  <% if @lendable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lendable.errors.count, "error") %> prohibited this lendable from being saved:</h2>

      <ul>
      <% @lendable.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :owner %><br />
    <%= f.text_field :owner %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
